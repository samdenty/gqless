module.exports = {
  // experimental: {
  // reactMode: 'concurrent',
  // },

  // Set to `true` to test consistency.
  // Set to `false` to test minimal renders possible.
  reactStrictMode: false,

  future: {
    webpack5: true,
  },
};
