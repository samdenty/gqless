---
id: variables
title: Variables
---

It's good practise to use variables whenever arguments change based upon state.

## Example

```jsx
import { graphql, useVariable } from '@gqless/react'

const App = graphql(() => {
  const [id, setId] = useVariable('bob')

  return <div>{query.user({ id }).name}</div>
})
```
